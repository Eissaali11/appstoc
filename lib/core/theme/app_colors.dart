import 'package:flutter/material.dart';

class AppColors {
  // الألوان الأساسية
  static const primary = Color(0xFF18B2B0); // تركواز
  static const primaryDark = Color(0xFF16A09E);
  
  // الخلفيات
  static const background = Color(0xFFF8FAFC);
  static const backgroundDark = Color(0xFF0F172A); // خلفية داكنة
  
  // الأسطح
  static const surface = Color(0xFFFFFFFF);
  static const surfaceDark = Color(0xFF1E293B); // سطح داكن
  static const cardColor = Color(0xFF334155);
  
  // ألوان الحالة
  static const error = Color(0xFFEF4444); // أحمر للخطأ
  static const success = Color(0xFF22C55E); // أخضر للنجاح
  static const warning = Color(0xFFF59E0B); // برتقالي للتحذير
  static const accentGreen = Color(0xFF22C55E);
  static const accentOrange = Color(0xFFF59E0B);
  static const accentRed = Color(0xFFEF4444);
  
  // النصوص
  static const textPrimary = Color(0xFF1E293B);
  static const textSecondary = Color(0xFF64748B);
  static const textLight = Color(0xFFFFFFFF);
  
  // الحدود
  static const border = Color(0xFFE2E8F0);
  
  // ألوان إضافية للـ Dashboard
  static const purpleGradient = [Color(0xFF6366F1), Color(0xFF4F46E5)];
  static const orangeGradient = [Color(0xFFF59E0B), Color(0xFFD97706)];
  static const greenGradient = [Color(0xFF22C55E), Color(0xFF16A34A)];
}
