# 🔍 دليل استكشاف أخطاء تسجيل الدخول

## ✅ التحسينات المضافة

### 1. تحسين AuthInterceptor
- لا يضيف `Authorization` header في طلب تسجيل الدخول
- يضيف `Authorization` header فقط للطلبات الأخرى

### 2. تحسين معالجة الاستجابة
- يدعم الاستجابات مع أو بدون `success` field
- معالجة أفضل لأنواع مختلفة من الاستجابات (Map, String)
- تسجيل مفصل للاستجابات

### 3. تحسين معالجة الأخطاء
- رسائل خطأ أوضح
- معالجة خاصة لأخطاء SSL/CORS
- معالجة خاصة لأخطاء الاتصال

---

## 🔍 خطوات الاستكشاف

### 1. تحقق من Console Logs

عند محاولة تسجيل الدخول، يجب أن ترى في الـ Console:

```
🔐 محاولة تسجيل الدخول...
📍 URL: https://fcf0121e-0593-4710-ad11-105d54ba692e-00-3cyb0wsnu78xa.janeway.replit.dev/api/auth/login
👤 Username: Rasco8273
📤 Request Body: {username: Rasco8273, password: ***}
```

**إذا رأيت هذه الرسائل:**
- ✅ الطلب يتم إرساله بشكل صحيح
- ✅ Base URL صحيح

**إذا لم تر هذه الرسائل:**
- ❌ تحقق من أن `AuthController.login()` يتم استدعاؤه
- ❌ تحقق من أن `LoginForm` يعمل بشكل صحيح

---

### 2. تحقق من الاستجابة

**نجاح (200/201):**
```
✅ Status Code: 200
📦 Response Data: {...}
📦 Response Type: _InternalLinkedHashMap<String, dynamic>
✅ تسجيل الدخول ناجح!
```

**فشل (401):**
```
❌ DioException Type: DioExceptionType.badResponse
❌ Status Code: 401
❌ Response Data: {...}
```

**خطأ في الاتصال:**
```
❌ DioException Type: DioExceptionType.unknown
❌ Error: Failed host lookup
```

---

### 3. المشاكل الشائعة والحلول

#### المشكلة: "لا يمكن الوصول إلى الخادم"

**الأسباب المحتملة:**
1. الخادم غير متاح
2. Base URL خاطئ
3. لا يوجد اتصال بالإنترنت
4. مشكلة في CORS

**الحل:**
1. تحقق من Base URL في `lib/core/api/api_endpoints.dart`
2. اختبر الطلب مباشرة باستخدام Postman أو cURL
3. تحقق من أن الخادم يعمل

**اختبار سريع:**
```bash
curl -X POST https://fcf0121e-0593-4710-ad11-105d54ba692e-00-3cyb0wsnu78xa.janeway.replit.dev/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"Rasco8273","password":"كلمة_المرور"}'
```

---

#### المشكلة: "اسم المستخدم أو كلمة المرور غير صحيحة" (401)

**الأسباب المحتملة:**
1. كلمة المرور خاطئة
2. اسم المستخدم غير موجود
3. المستخدم غير نشط

**الحل:**
1. تحقق من اسم المستخدم: `Rasco8273`
2. تحقق من كلمة المرور
3. تأكد من أن المستخدم موجود في قاعدة البيانات

---

#### المشكلة: "تنسيق الاستجابة غير صحيح"

**الأسباب المحتملة:**
1. الاستجابة لا تحتوي على `user` أو `token`
2. تنسيق الاستجابة مختلف عن المتوقع

**الحل:**
1. تحقق من Console Logs لرؤية الاستجابة الفعلية
2. تحقق من أن الخادم يرسل الاستجابة بالشكل الصحيح:
   ```json
   {
     "success": true,
     "user": {...},
     "token": "..."
   }
   ```

---

#### المشكلة: "خطأ في شهادة SSL"

**الأسباب المحتملة:**
1. مشكلة في شهادة SSL للخادم
2. مشكلة في إعدادات Flutter

**الحل:**
1. تحقق من أن الخادم يستخدم HTTPS بشكل صحيح
2. في حالة التطوير، يمكن إضافة:
   ```dart
   // في android/app/src/main/AndroidManifest.xml
   android:usesCleartextTraffic="true"
   ```

---

## 📋 قائمة التحقق

قبل الإبلاغ عن مشكلة، تأكد من:

- [ ] Base URL محدث في `api_endpoints.dart`
- [ ] الخادم يعمل ومتاح
- [ ] اسم المستخدم صحيح: `Rasco8273`
- [ ] كلمة المرور صحيحة
- [ ] يوجد اتصال بالإنترنت
- [ ] تم فحص Console Logs
- [ ] تم اختبار الطلب مباشرة (Postman/cURL)

---

## 🧪 اختبار مباشر

### باستخدام cURL
```bash
curl -v -X POST https://fcf0121e-0593-4710-ad11-105d54ba692e-00-3cyb0wsnu78xa.janeway.replit.dev/api/auth/login \
  -H "Content-Type: application/json" \
  -H "Accept: application/json" \
  -d '{
    "username": "Rasco8273",
    "password": "كلمة_المرور"
  }'
```

### باستخدام Postman
1. Method: `POST`
2. URL: `https://fcf0121e-0593-4710-ad11-105d54ba692e-00-3cyb0wsnu78xa.janeway.replit.dev/api/auth/login`
3. Headers:
   - `Content-Type: application/json`
   - `Accept: application/json`
4. Body (raw JSON):
   ```json
   {
     "username": "Rasco8273",
     "password": "كلمة_المرور"
   }
   ```

---

## 📝 معلومات مفيدة للإبلاغ عن المشاكل

عند الإبلاغ عن مشكلة، قدم:

1. **Console Logs الكاملة** (من 🔐 إلى ❌)
2. **Status Code** (إن وجد)
3. **Response Data** (إن وجد)
4. **نوع الخطأ** (DioException Type)
5. **نتيجة الاختبار المباشر** (Postman/cURL)

---

## 🔗 ملفات ذات صلة

- `lib/core/api/api_endpoints.dart` - Base URL
- `lib/core/api/interceptors/auth_interceptor.dart` - Interceptor
- `lib/features/auth/data/datasources/auth_remote_data_source.dart` - Data Source
- `lib/features/auth/presentation/controllers/auth_controller.dart` - Controller

---

## 💡 نصائح

1. **استخدم Console Logs:** جميع الطلبات والاستجابات يتم تسجيلها
2. **اختبر مباشرة:** استخدم Postman أو cURL للتحقق من أن الخادم يعمل
3. **تحقق من Base URL:** تأكد من أنه محدث وصحيح
4. **تحقق من Headers:** تأكد من أن `Content-Type` و `Accept` موجودة
